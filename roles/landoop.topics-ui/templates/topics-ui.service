[Unit]
Description=Landoop Kafka Topics UI
After=network.target

[Service]
Type=simple
User=cp-kafka
WorkingDirectory=/srv/topics-ui
ExecStart=/usr/local/bin/http-server -p 8085 .
Restart=always

[Install]
WantedBy=multi-user.target
