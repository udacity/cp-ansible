[Unit]
Description=Landoop Kafka Connect UI
After=network.target

[Service]
Type=simple
User=cp-kafka
WorkingDirectory=/srv/connect-ui
ExecStart=/usr/bin/http-server -p 8084 .
Restart=always

[Install]
WantedBy=multi-user.target
